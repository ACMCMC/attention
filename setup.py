# Setup file for the package

from setuptools import setup, find_packages

setup(
    name='attention',
    version='0.1.0',
    description='Attention evaluation for NLP models',
    author='Aldan Creo',
)
